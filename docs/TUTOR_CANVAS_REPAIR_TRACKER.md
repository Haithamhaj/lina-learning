# Tutor / Canvas Repair Tracker

Updated: 2026-09-16. Status authority: the latest explicit Product Owner decisions, verified implementation, and the evidence identified below.

This is the single operational status register for the repairs discussed in the September 2026 Tutor/Canvas review. It is not a new architecture, a replacement for TASKS.md, or authorization to execute every listed item. PROJECT_STATE.md holds only the current focus and a pointer here.

## Current decisions

- R01-R15 and V01 are closed locally. The active non-call-architecture verification slice is CTX-01, CTX-02 and LEARN-LOOP; it must preserve the existing authority boundaries and use synthetic/disposable evidence only.
- Keep the One Call / multi-call / foreground-background study separate and DEFERRED. Do not implement B0/B1, remove candidate_metadata, move classifications, or add agents in this repair slice.
- Use English for Codex prompts, implementation instructions, and handoffs. User-facing discussion remains Arabic unless requested otherwise.
- Preserve implemented Canvas lifecycle, stale-base guards, Fact identity compatibility, source/safety/ownership boundaries and existing learning-data authority.
- Record an unresolved proof as unresolved proof, not automatically as a broken component or authorization to rebuild it. Do not turn Clerk or deferred studies into new prerequisites for finishing the approved teaching patch.
- No new authorization for paid calls, production data, push, merge or deployment is granted by this tracker. Ordinary safe local setup follows the existing authorization; do not ask the owner to reconfirm it.

## Status and update rules

| Status | Meaning |
| --- | --- |
| OPEN | Required work or verification has not been closed; this does not imply the entire component is absent. |
| IN_PROGRESS | Active execution has actually started, with evidence of work. |
| PARTIAL | Some approved requirements are implemented; named requirements remain. |
| IN_REVIEW | Implementation or a proposed decision exists; the stated acceptance is not yet established. |
| BLOCKED | A precise prerequisite prevents this row from proceeding; name that prerequisite. |
| DEFERRED | Explicitly outside the current execution slice; preserve it without treating it as DONE. |
| DONE | The exact acceptance scope of the row is satisfied with evidence. Local DONE never means deployed or proven learning benefit. |

After each verified slice, update the affected row, date, evidence and remaining action; include the changed IDs in the progress reply. Preserve IDs and completed evidence. Reopen a DONE row only for a concrete regression or missed requirement. Do not infer completion from an agent's summary or a green test count alone.

## Main repair register

Priority is 1-5; 5 is highest. All dates are last status-review dates, not promises of execution. Evidence IDs resolve below. Separate implementation completion from behavioral acceptance.

| ID | Priority | Scope / outcome | Status | Evidence or remaining requirement | Next action | Updated |
| --- | --- | --- | --- | --- | --- | --- |
| TRACK-01 | 5 | One repair register and English Codex handoffs | DONE | This file; pointers and update rules in AGENTS.md and PROJECT_STATE.md | Maintain this register; do not create competing status lists | 2026-09-16 |
| PED-BASE | 5 | Preserve the previously approved subset of the pedagogy reference | DONE | E01; imported in commit b752798; shared teaching block remains present | Preserve correct scaffolding, fading, application and source/learning boundaries while completing AB-B | 2026-09-16 |
| AB-B | 5 | Complete the additional approved teaching/definition/visual redline | DONE | E10 independently confirms R03 normalized-exact against E02, remaining redline semantics installed, single loaded V2 guide, and shared Chat/Studio instruction source. Source implementation accepted; final test compatibility is tracked in V01. | Keep V02/E2E/UX-BUFFER as separate gates | 2026-09-16 |
| BEH-PLAY | 5 | Implement approved guidance for useful attempts, curiosity, manageable challenges, playful participation and adaptation without a fixed lesson script | DONE | R03 instruction implementation verified against E02 in E10; this DONE is not acceptance of real teaching behavior. | V02 remains the separate real multi-turn behavior gate | 2026-09-16 |
| BEH-VISUAL | 5 | Appropriate proactive Canvas and actual picture requests honored without requiring the Student to know Canvas | IN_REVIEW | E16 proved an explicit visual request was selected correctly but delivered defectively. E20 adds a clean negative case: a Science process (evaporation/water cycle) stayed text-only across several turns and repeated confusion, with no proactive Canvas request. | Repair delivery blockers first, then verify proactive visual selection on Science/process cases; V2 generated images remain separate. | 2026-09-20 |
| A01 | 5 | Authoritative Canvas Run/Job/Scene projection reaches API and Daily | DONE | E03/E05; composition view and terminal/API contracts verified locally | Preserve; reopen only for a concrete affected regression | 2026-09-16 |
| A02 | 5 | Immediate waiting/update UI, elapsed time, reload/reconnect, stale-response ordering and usable chat after Tutor reply | DONE | E03/E05/E06; fixture-backed authenticated browser proof, not real generation proof | Preserve working waiting UX and ordering | 2026-09-16 |
| A03 | 5 | Chat/Studio intent continuity, supported STEP, meaningful replacement and server-owned stale-base guard | DONE | E03/E05; Run/Scene base captured before inference; no duplicate composition for supported continuation | Preserve both origins and legitimate visual changes | 2026-09-16 |
| FIX-FACT | 5 | Current/legacy Fact-key readers agree; exact filtered-catalogue ownership remains | DONE | E05; unchanged saved Luna output replay creates one causal replacement; no paid call | Preserve exact identity, bounds and historical records | 2026-09-16 |
| FIX-REJECT | 5 | A rejected visual does not leak a promise or partial learning writes; completed provider execution remains recorded | DONE | E05/E06; Daily rejection path and next attempt verified locally | Preserve integrity; UX effect of general buffering is tracked separately | 2026-09-16 |
| UI-COPY | 3 | Arabic/English connection, error, composer and voice copy | DONE | E06; affected authenticated Daily cases passed | Do not reopen for unrelated interface redesign | 2026-09-16 |
| UI-RECOVER | 4 | Automatic retry after a failed recovery, one feed and unmount cleanup | DONE | E05/E06; deterministic and affected authenticated browser evidence | Preserve the one-loop recovery behavior | 2026-09-16 |
| CTX-01 | 5 | Profile, Facts/Memory, LI and source grounding reach the appropriate turn and remain distinct authorities | DONE | E13: 323 focused synthetic/disposable tests verify actual Tutor payload/context construction: bounded Core Profile and absent-field behavior; current Personal Memory with current-turn precedence; subject-gated LI; and filtered Canvas catalogue without raw Memory/LI. | V02 remains the separate real teaching-quality gate; no Fact use is required when it adds no value. | 2026-09-16 |
| CTX-02 | 4 | Immediate/recent/semantic recall and conditional Concept history preserve useful multi-turn continuity and language switches | DONE | E13: actual context/payload tests cover immediate/recent/semantic/pinned recall, deterministic capacity trimming, Structured Segment State, Arabic/English neutral-turn and explicit-switch guidance, CONTINUE-only Concept history, and NEW_SEGMENT isolation. | V02 remains separate from deterministic continuity/payload acceptance; do not move Segment/Subject/Concept processing. | 2026-09-16 |
| CLASS-01 | 4 | Implement approved classification definitions and preserve the existing source-linked validation boundaries | DONE | R11-R13 definitions installed with registry-v1 keys retained; E09/E10 focused tests pass. Behavioral classification quality remains V02. | V02 remains the behavior-quality gate | 2026-09-16 |
| LEARN-LOOP | 5 | Student source -> review -> authorized Evidence/State/Patterns -> later Tutor use | DONE | E13: fixture-model/disposable PostgreSQL coverage verifies raw-source authority, optional bounded Candidate hints, validated Review-to-Finalization lineage, guarded State/Pattern/card projection, later scoped Tutor context, and retry idempotency. | Real learner use, live teaching quality and any historical reprocessing remain outside this local technical closure. | 2026-09-16 |
| V01 | 5 | Local acceptance for the completed redline on the actual assembled Chat and Studio requests | DONE | E09 focused checks; E10 found only stale V1 test expectations; E11 corrected those assertions; E12 independently reran the affected slice plus the full disposable PostgreSQL gate: 1502 passed, 4 known baseline failures, 12 skipped, with repository truth and diff checks passing. | V02/E2E-01/UX-BUFFER remain separate acceptance gates; no pedagogy rollback. | 2026-09-16 |
| REPAIR-SLICE-01 | 5 | Learner-facing reliability and interaction integrity: SESSION-LIFECYCLE-01 + TURN-ARB-01 + CANVAS-SELECT-01 + CANVAS-QUEUE-01 | DONE | E21 implemented the bounded slice; E22 found three recovery gaps; E23 corrected them; E24 independently re-reviewed the source and reran the focused E23 PostgreSQL regressions plus typecheck/repository checks. No blocker remains inside this local slice. | Preserve the uncommitted reviewed tree. Merge/deploy/live acceptance remain separate Product Owner decisions. | 2026-09-20 |
| REPAIR-SLICE-02 | 5 | Canvas delivery integrity: CANVAS-RENDER-01 + CANVAS-ANSWER-LEAK-01 + VISUAL-RECOVERY-01 | DONE | E25 implemented the bounded slice; E26 found two final gaps; E27 corrected them; E28 independently reran the exact renderer/presentation and recovery/stale/idempotency gates and found no remaining blocker in local scope. | Preserve the accepted uncommitted tree. Real Luna/live E2E acceptance, merge and deployment remain separate Product Owner decisions. | 2026-09-20 |
| V02 | 5 | Real multi-turn teaching behavior, including application after correction and independent current behavior | IN_REVIEW | E16: controlled live use on the current guidance verified DID_NOT_HELP detection and a later explicit visual representation change; one failed-method turn still behaved mostly as re-explanation, and Canvas defects contaminated later turns. | Continue owner-led natural-topic discovery in fresh sessions; do not score pedagogy after a confirmed Canvas/transport contamination point. | 2026-09-17 |
| E2E-01 | 5 | Real Tutor -> real Canvas Specialist -> visible Scene -> Student action -> Tutor continuation | IN_REVIEW | E25-E28 establish local integrated technical acceptance using fixture/mock providers plus actual Chrome rendering: Scene settlement, visible Plot, truthful interaction state, Tutor continuation, and bounded recovery paths are covered. This is not real Luna/live generation acceptance. | Preserve local evidence; rerun controlled live E2E after an authorized deployment. | 2026-09-20 |
| PERF-01 | 4 | Real Canvas queue, creation/reuse, review/refinement and first useful visual latency | IN_REVIEW | E15/E16 established slow live behavior; E20 adds a 58.2s successful Primary Tutor outlier on a simple motivational turn, while later Science turns were mostly ~8-11s. The earlier 300s transport hang is separately fixed under LIVE-TRANSPORT-01. | Profile request construction/model/terminal-buffer phases on representative live turns; do not treat the 58s outlier as normal or blame Canvas without phase evidence. | 2026-09-20 |
| UX-BUFFER | 4 | Product decision about buffering every structured Tutor reply until validation | IN_REVIEW | E16 confirms the live UX effect: because provisional Tutor text remains buffered, first useful visible text is delayed until terminal completion; observed successful turns included ~35-47s waits. E15 bounded failure transport but did not change buffering semantics. | Continue live observation; treat transport reliability and product-perceived latency as separate decisions. | 2026-09-17 |
| LIVE-TRANSPORT-01 | 5 | A slow/active Tutor provider stream must terminate predictably instead of hanging until Cloud Run kills the request | DONE | E15: live 300s 504 reproduced; provider total stream deadline, Next proxy margin and structured SSE error handling were added and deployed. A later live failed-method turn completed beyond 30s instead of reproducing the 300s hang. | Preserve the bounded failure contract; keep monitoring for new transport failures. | 2026-09-17 |
| CANVAS-RENDER-01 | 5 | Typed MATH_BOARD plot expressions must be visibly rendered, not merely listed as text below an empty graph | DONE | E25/E28: bounded no-eval `y=c` / `y=mx+b` parsing, clipping, stable expression identity and explicit unsupported `(not drawn)` fallback are independently verified; the three E16 live expressions render as distinct lines and Plot SELECT remains suppressed. | Preserve the bounded renderer; do not expand into a general CAS. | 2026-09-20 |
| CANVAS-SELECT-01 | 5 | SELECT must represent the Student's actual target, never silently select `elements[0]` | DONE | E21/E24: generic first-element binding is removed; FOCUS/SELECT are target-bound, zero targetable elements emit no SELECT, hidden Plot targets are suppressed, and SUBMIT remains block-level. Independent source review found no remaining selection-truthfulness blocker. | Preserve; CANVAS-RENDER-01 remains separate. | 2026-09-20 |
| CANVAS-QUEUE-01 | 5 | Prevent repeated/ambiguous Canvas actions from creating an interaction storm while Tutor feedback is pending | DONE | E21/E23/E24: synchronous browser gating and the shared server lane prevent duplicate Tutor-triggering interactions; exact replay returns the same interaction, replayed PENDING resumes that exact Tutor stream, RUNNING/terminal states do not restart it, and server claim remains atomic. Independent focused replay proof passed. | Preserve the one-lane design; no general queue/orchestrator. | 2026-09-20 |
| CANVAS-ANSWER-LEAK-01 | 4 | Classification/choice UI must not reveal the answer through initial grouping before the Student acts | DONE | E25/E27/E28: classification/grouping/matching/relation presentation reads reduced learner state rather than authoring solution semantics; categories and choices are separated. Unsupported generic ORDERING fails closed with no authored order, no `Current order`, no Submit/action controls, and Tutor-chat fallback. | Preserve. Any future actionable generic ORDERING requires an approved durable order contract. | 2026-09-20 |
| TURN-ARB-01 | 5 | A Canvas interaction must not silently erase/supersede an in-flight Student Chat request | DONE | E21/E24: one LearningSession-row-locked foreground lane arbitrates Daily Chat and Tutor-triggering Canvas admissions; competing work fails before admission, prior admitted Chat output is not discarded by later Canvas state, and terminal status is explicit. Independent concurrency review/tests found no remaining blocker. | Preserve the server-owned one-foreground-lane invariant. | 2026-09-20 |
| VISUAL-RECOVERY-01 | 5 | When the Student explicitly says the visual is missing/broken and seeing it is necessary, recover/repair the visual rather than substitute text by default | DONE | E27/E28: deterministic outputs traverse real TutorRuntime persistence and Canvas admission: FAILED same-need → exact-bound RETRY; PENDING/RUNNING → no duplicate; ready but client-missing → Reload Workspace/no composition; semantic insufficiency → exact Scene-bound REPLACE_SCENE; stale decisions remain rejected. | Local technical closure only. Real Luna decision quality remains V02/live acceptance. | 2026-09-20 |
| PED-ADAPT-01 | 4 | DID_NOT_HELP should cause a substantively different teaching move when the first explanation did not help | OPEN | E16 showed correct DID_NOT_HELP classification followed by mostly re-explanation. E20 adds a clean Science case where an explicit “ما فهمت...” turn remained CONTINUATION and again produced explanation rather than a clearly different learning move. | Repair adaptation semantics with regression cases that distinguish targeted clarification from true method failure; preserve natural clarification when the Student only asks about one phrase. | 2026-09-20 |
| SESSION-LIFECYCLE-01 | 5 | Expired/non-resumable Daily sessions must not leave an apparently usable composer that repeatedly posts to a dead session | DONE | E21/E23/E24: stale 404/409 recovery preserves visible transcript and draft, replacement creation is retry-idempotent for the owned ended session, unknown/foreign/open references fail closed, old session-scoped assets remain historical only, and a still-local unsent file survives rebind for the replacement session. Independent focused regressions passed. | Preserve; historical transcript product/API remains outside scope. | 2026-09-20 |
| CONV-MOMENTUM-01 | 4 | During an active child learning sequence, a completed Tutor turn should leave a clear next affordance when the activity is not actually finished, without forcing a question after every reply | OPEN | E17/E18 proved the dead-end pattern in Arabic grammar. E20 confirms it cross-subject in Science: multiple explanatory turns ended with no learner action, check, choice, or completion cue, requiring the learner to invent the continuation. | Cross-subject evidence is now sufficient to repair: preserve legitimate stopping points, but unfinished guided learning must leave a clear next affordance. | 2026-09-20 |
| STRATEGY-FIDELITY-01 | 4 | A declared teaching strategy must be reflected in the learner-visible move, especially EXPLAIN_THEN_CHECK | OPEN | E20: several clean Science turns were persisted as `EXPLAIN_THEN_CHECK` but ended with explanation/summary only: no actual check, learner attempt, guided check, question, or suggested next action. | Add behavioral contract/regression coverage so strategy labels and visible teaching moves agree; do not force a check when another strategy is selected. | 2026-09-20 |
| PERSONALIZATION-RELEVANCE-01 | 3 | Personalization should feel relevant and non-repetitive rather than inserting the same learner fact/strategy into loosely related replies | IN_REVIEW | E20: a multiplication-related personalization phrase was reused in more than one motivation/study-strategy response. It may be valid context, but repetition felt potentially forced and the source authority was not audited in this review. | Review additional live cases and source lineage before changing selection rules; do not remove valid personalization based on one session. | 2026-09-20 |
| IMG-V2-01 | 3 | V2 generated educational images for Science visual/process learning | PLANNED | Product Owner decisions: initial eligibility is Science only and limited to shape/structure/process/scene; image generation can follow an explicit learner request within that scope or a contextual CTA; Tutor may signal image usefulness but server/system eligibility rules own the actual CTA/generation decision and enforce subject/category/quota limits; CTA is never permanent; Canvas hosts delivery; allowance is 10 successful visible generated images per learner/day, excluding failed/internal retries and redisplay of the same generated image. When the allowance is exhausted, hide the CTA for the rest of the day; explicit image requests receive a simple quota-exhausted message and fall back to structured Canvas/drawing or verbal explanation. The allowance resets at local midnight using the learner's stored timezone. Generated images are illustrative learning aids rather than literal scientific reference images; exact scientific text/labels must be grounded in trusted information rather than inferred from pixels. Default output is an educational image with simple labels/arrows/stages when useful. | Eligibility scope/authority, quota UX/reset, high-level scientific grounding, and annotated-output default are decided. Remaining design: provider/model selection, questionable-output rejection/retry behavior, quota persistence/enforcement, exact Canvas annotation implementation, and V2 acceptance. Current live-repair work remains separate. | 2026-09-20 |
| AUTH-01 | 3 | Initial Daily token-refresh 401 followed by successful retry | DEFERRED | E06 reproduces the boundary twice; latest owner direction parks Clerk outside Tutor repairs | Separate auth issue; do not block approved pedagogy completion on it | 2026-09-16 |
| CALLS-01 | 5 | One Call vs foreground/background responsibility study; Current/B0/B1 alternatives | DEFERRED | E08 report/addendum retained. E20 adds live cost evidence for the later study: a trivial one-message greeting session still triggered Segment Evidence and Personal Facts Luna calls at session close. | Keep deferred while current product repairs are active; when resumed, include background-call eligibility/cost suppression for trivial sessions. No candidate_metadata change now. | 2026-09-20 |
| SAVE-01 | 4 | Preserve the completed repair in an exact reviewed branch commit | DONE | E14 preserves the reviewed branch snapshot. The owner later explicitly authorized merge/live deployment; E15 records the controlled live update and transport hotfix. | Preserve immutable SHAs/evidence; further fixes discovered in live testing require their own verified commits. | 2026-09-17 |
| RELEASE-01 | 5 | Merge/deploy and production acceptance | IN_REVIEW | E15: owner explicitly authorized main update and controlled Cloud Run live deployment. Current live pilot is running, but E16 found multiple critical product defects; deployment is not product acceptance. | Continue controlled discovery; repair confirmed blockers before declaring production acceptance. | 2026-09-17 |

## R01-R15 completion register (AB-B)

Authoritative wording: E02. Semantic equivalence may satisfy a row when demonstrated; literal text matching alone is not acceptance. Do not duplicate text that is already implemented correctly. The previous blanket B01/B02 completion claim is superseded by the source-to-redline review below.

| ID | Priority | Approved change | Status | Current evidence / missing work | Verification before DONE | Updated |
| --- | --- | --- | --- | --- | --- | --- |
| R01 | 4 | Tutor identity includes understanding and meaningful participation | DONE | Shared role paragraph already updated in runtime.py | Retain in assembled Chat and Studio instructions | 2026-09-16 |
| R02 | 5 | Profile calibrates abstraction, chunk size, pacing, language and examples, not mastery | DONE | Shared Core Context paragraph already expanded | Retain authority and absent-field protections in both origins | 2026-09-16 |
| R03 | 5 | Replace the teaching block: actual practice, inviting choices/play, tracking failed explanations and reusable strategies | DONE | E02 wording installed in `runtime.py`; E09 assembly/contract regression passed | V02 validates real teaching quality separately | 2026-09-16 |
| R04 | 3 | Context-appropriate notation rather than fixed Grade-5 wording | DONE | E02 wording installed; E09 assembled instruction check passed | None for static scope | 2026-09-16 |
| R05 | 4 | Optional contextual actions; no repeated menu or new visual merely for status | DONE | E02 wording installed; E09 assembled instruction check passed | UX-BUFFER remains separate | 2026-09-16 |
| R06 | 5 | Missing necessary source meaning prevents all dependent visual requests, not only the legacy order | DONE | E02 wording installed; E09 source/contract instruction check passed | Real source/Canvas E2E remains E2E-01 | 2026-09-16 |
| R07 | 4 | Move visual-field rules into one loaded visual guide and remove duplicated shared tail | DONE | `visual-guidance-v2.md` is the sole loaded Tutor guide; E09 guide/assembly tests passed | Historical v1 stays readable, not loaded | 2026-09-16 |
| R08 | 5 | Prepared legacy exact-match limits do not prohibit grounded Full-Power Canvas | DONE | E02 legacy-only wording installed in dynamic context; E09 regression passed | Real generated Canvas remains E2E-01 | 2026-09-16 |
| R09 | 4 | Dynamic Studio context interprets action/state; supported STEP is continuation; routing has one owner | DONE | E02 dynamic wording installed; E09 Studio-context regression passed | Real interaction delivery remains E2E-01 | 2026-09-16 |
| R10 | 3 | No visual catalogue means canvas_visual_context_selection=null | DONE | Current-field wording installed; E09 assembly/capacity checks passed | None for static scope | 2026-09-16 |
| R11 | 4 | Delivery feedback is not method failure; purely operational replies need not invent teaching labels | DONE | E02 calibration clarification installed; E09 runtime contract regression passed | V02 remains behavior gate | 2026-09-16 |
| R12 | 5 | Complete all approved Mode and Strategy clarifications using existing enum values | DONE | E02 descriptions installed without enum changes; E09 contract regression passed | V02 remains behavior gate | 2026-09-16 |
| R13 | 5 | Complete the seven Method descriptions, preserving registry-v1 historical meaning | DONE | E02 descriptions installed; `teaching-method-registry-v1` and keys unchanged; E09 tests passed | No backfill or historical reinterpretation | 2026-09-16 |
| R14 | 5 | One unified visual guide: proactive, useful, truthful, source-grounded, Full-Power compatible | DONE | `visual-guidance-v2.md` loaded once; E09 guide/assembly regression passed | V02/E2E remain separate | 2026-09-16 |
| R15 | 4 | Retain v12 canvas_change_intent contract and finalize its schema-description consistency | DONE | Existing nullable enum/readers retained; bounded non-Evidence description and E09 contract assertion added | Historical v11 reader remains unchanged | 2026-09-16 |

## Evidence and references

E01: `docs/TUTOR_PEDAGOGY_REFERENCE.md` and `TASKS.md` section TUTOR-PEDAGOGY-REFINE-01. Reference SHA-256: `5e50985b7bc2a8d7842a1193e8ae0384c6c81ee4abdbcf44b123ad8123f122ac`. Previously approved subset arrived in `b752798`; the full document remains an authoring/evaluation reference, not a runtime payload. Selected principles cover scaffolding, fading, worked reasoning, actionable correction, representation mapping, meaningful application and appropriate variation/recall. Other research rows were intentionally retained as reference, not approved as new runtime features.

E02: Approved A/B handoff at `/Users/haitham/Downloads/LINA_TUTOR_CANVAS_AB_HANDOFF_2026-09-16/docs/PROMPT_REDLINE.md`, R01-R15; associated design and acceptance cases in the same handoff. This is the detailed completion specification, not text to inject wholesale into the model.

E03: Source review of `services/tutor/runtime.py`, `teaching_decisions.py`, `teaching_methods.py`, `candidate_events.py`, `services/studio/interactions.py`, `services/studio/agent/admission.py`, `services/studio/canvas_brief.py`, and `runtime/tutor/visual-guidance-v1.md`. Candidate base `181f0fafe5dcd469f64fe2440c8bec7f96a1402d`; baseline `b909e2d8c0878453604d5213cb349dd71651d8db`. Local closure evidence is scoped to that source plus the recorded repair diff, not the deployed application.

E04: Limited earlier live evaluation under `/private/tmp/lina-tutor-canvas-ab-eval-181f0fa/`. Seven cases per version; all 14 successful provider outputs had null candidate_metadata. Not proof of absent extraction value or of completed new pedagogy. `evidence/blind/review-pairs.json` remains an uncompleted human-review input. Do not rewrite those results after subsequent repairs.

E05: Targeted repair report `/private/tmp/lina-tutor-canvas-ab-targeted-fix-181f0fa-26912a7b/RESULTS.md` and manifest; original replay fixture at `tests/fixtures/tutor_canvas_ab_v02_meaningful_representation_change.json`. Prior independent verification reproduced 1500 passed / the same 4 baseline failures / 12 skipped, and validated exact saved-output replay, typecheck, selected frontend tests and repository truth. These tests were not rerun merely to create this tracker.

E06: `/private/tmp/lina-tutor-canvas-ab-final-acceptance-26912a7b/evidence/browser-authenticated/results.json` and the associated sanitized report. Seven local authenticated fixture scenarios passed; initial-load auth race separately recorded. No real Canvas worker/generation was exercised in that round. Evidence applies to tested behavior/source, not all future changes.

E07: `/private/tmp/lina-tutor-canvas-ab-final-acceptance-26912a7b/evidence/streaming/timing-results.json`. Controlled-provider event ordering only; not a Luna latency benchmark or product approval of general buffering.

E08: Updated responsibility report `/Users/haitham/.codex/visualizations/2026/09/16/01a0ab51-5f65-7fb0-b1ed-05c2eb7c0ab1/primary-tutor-background-responsibilities-report-ar.md`. Its corrective addendum governs factual corrections in that report. Current/B0/B1 remain proposals. The owner's later instruction explicitly defers the entire call study until after repairs.

E09: Local R01-R15 closure, 2026-09-16: `PYTHONPATH=. uv run --with-requirements apps/api/requirements.txt --with pytest python -m pytest tests/test_tutor_runtime_contract.py tests/test_teaching_methods.py tests/test_tutor_context_capacity.py tests/test_process_visual_awareness.py -q` → `58 passed`; `PYTHONPATH=. uv run --with-requirements apps/api/requirements.txt --with pytest python -m pytest tests/test_studio_tutor_context.py tests/test_tutor_runtime_scenarios.py -q` → `90 passed`. Both use the current assembled `TUTOR_SHARED_INSTRUCTIONS`; Studio imports the same shared instruction source. Before/after retention fixture at the 64,000-character guard retained every selected unit (`0` dropped): `44,683 → 44,889` serialized characters. Minimal request was `42,226 → 42,432`; assembled instructions `19,792 → 20,055`; schema serialized size after closure `14,257`. The four known full-suite baseline failures were not rerun and remain separately recorded in E05/project state. No provider, worker, Clerk, production DB or paid evaluation ran.

E10: Independent review on 2026-09-16. Product/test content manifest SHA-256 b97c8763fcfd7164c0b36cc1d7676c68f90aee31c9d658f5b953d892fd1b4bd2 (sorted changed paths under apps/services/runtime/tests; UTF-8 path + NUL + exact file bytes + NUL, against HEAD 181f0fafe5dcd469f64fe2440c8bec7f96a1402d). R03 matches the approved replacement after whitespace normalization; the pedagogy-reference SHA remains E01; only visual-guidance-v2.md is loaded. Re-ran the six E09 files with the existing project Python: 148 passed. Repository-truth and diff checks passed. Final disposable PostgreSQL suite via scripts/test_postgres.py test: 1499 passed, 7 failed, 12 skipped; exit 1. The three additional failures are test_primary_tutor_loads_only_its_accepted_visual_guidance in tests/test_canvas_specialist_skill.py, and test_normal_chat_uses_actual_accepted_scene_one_primary_call / test_runtime03_exact_source_survives_later_record_only_focus in tests/test_process_visual_awareness_postgres.py. All stop at obsolete VISUAL_GUIDANCE_V1 assertions while the approved V2 is supplied. The four baseline-known failures retain the same names. Raw local log: /private/tmp/lina-r01-r15-final-review.GXqIuj. No product code, tests, model calls or production data changed during this review; disposable resources were removed. V01 remains IN_REVIEW pending test-only compatibility closure. No live-behavior or release acceptance is implied.

E11: V01 test-only compatibility closure, 2026-09-16. Updated only `tests/test_canvas_specialist_skill.py` and the shared `Capture.stream` expectation in `tests/test_process_visual_awareness_postgres.py`: assembled Primary Tutor instructions must contain `VISUAL_GUIDANCE_V2` exactly once and must not contain `VISUAL_GUIDANCE_V1`. Specialist-pack exclusion, semantic-source, lineage, current-interaction and later-record-only-focus assertions remain intact. With the disposable PostgreSQL database: the three named affected tests passed (`3 passed`); canonical `npm run test:python` then returned exit `1` with `1502 passed, 4 failed, 12 skipped` in 72.48s. The four failures are the documented baseline failures from E10, with no remaining stale-guide failure. `python3 scripts/check_repository_truth.py` passed and `git diff --check` passed. The canonical test script removed its disposable container and volume in its `finally` cleanup. No product code, provider/model call, Clerk, production database, commit, push, merge or deployment occurred. V01 is DONE; V02, E2E-01 and UX-BUFFER remain separate.

E12: Independent V01 re-verification, 2026-09-16: the affected guidance assertions were rerun locally (`6 passed, 2 skipped` without PostgreSQL enablement), followed by a fresh disposable PostgreSQL full gate using `scripts/test_postgres.py test` → `1502 passed, 4 failed, 12 skipped` in 141.44s. The four failures are the same documented baseline failures: foundation table inventory, migration-head preflight, scheduled future Grade, and Personal Facts reconciliation. No V1 guidance failure or new regression remained. `scripts/check_repository_truth.py` and `git diff --check` passed before the full gate. The disposable PostgreSQL container/volume was removed by the test runner. No provider, worker, Clerk, production DB, product edit, or paid model call was used for this verification.

E13: CTX-01/CTX-02/LEARN-LOOP local technical closure, 2026-09-16, against worktree HEAD `181f0fafe5dcd469f64fe2440c8bec7f96a1402d`. After `npm run test:db:reset`, the disposable PostgreSQL-focused command covering Tutor context/payload, Personal Memory, Segment continuity, canonical Concepts, semantic Review, Learning Review persistence, Session Finalization, Current State, learner-intelligence projection, Candidate persistence and runtime scenarios returned `259 passed` in 15.18s. A second focused command covering Primary Tutor instruction/payload, Canvas filtering, Candidate contract and Review jobs returned `64 passed` in 4.28s. The checks establish: Core Profile is presentation-only and absent fields stay absent; current Student context outranks personal-memory/history; ordinary LI requires a live subject while UNKNOWN does not fabricate one; Canvas gets the bounded filtered catalogue rather than raw Memory/LI; immediate/recent/semantic/pinned recall and structured state survive deterministic capacity reduction; neutral Arabic/English math turns retain the active language and explicit language switching retains the same context; conditional Concept history is CONTINUE-only and subject/student scoped; raw Student source remains authoritative; CandidateEvent is an optional hint; bare error is not a misconception; method selection is not effectiveness; Canvas interaction is only a source; Finalization requires completed compatible reviews and is idempotent. No product code changed. `npm run test:db:down` removed the disposable database; `python3 scripts/check_repository_truth.py` and `git diff --check` passed. No provider/paid call, worker, Clerk, browser, production database, historical reprocessing, commit, push, merge or deployment occurred. This is deterministic technical acceptance, not V02 teaching-quality, real Canvas E2E, buffering, CALLS-01 or AUTH-01 acceptance.

E14: Branch preservation authorization and exact-tree pre-commit verification, 2026-09-16. Product Owner explicitly approved committing the reviewed repair and pushing `codex/tutor-canvas-ab-01` to GitHub without merge or deployment. On the exact tree being committed: `python3 scripts/check_repository_truth.py` passed; `git diff --check` passed; the focused Tutor/Studio/teaching contract command returned `148 passed`; `npm run typecheck` passed; and a fresh disposable PostgreSQL `npm run test:python` gate returned `1502 passed, 4 failed, 12 skipped` in 140.30s. The only failures are the same documented baseline failures (foundation table inventory, migration-head preflight, scheduled future Grade, Personal Facts reconciliation); no new repair regression was observed. The test runner removed its disposable PostgreSQL resources. No provider, Clerk, worker, production database, merge, deployment, or paid call was used. The commit containing this E14 record is the V02 branch target; its pushed SHA is reported in the handoff.

E15: Controlled live update and Tutor transport hotfix, 2026-09-17. The owner explicitly authorized updating `main` and the Cloud Run live pilot. The reviewed repair reached live; an observed Tutor turn later hung until the 300s Cloud Run request timeout with no completed Tutor message/AIExecution. Root-cause review found provisional structured Tutor text intentionally buffered until terminal validation, Next's 30s proxy default, and no total Responses-stream deadline while internal SSE activity could keep the provider read alive. A bounded hotfix added a 90s total provider stream deadline, 180s Next proxy margin, and structured SSE error delivery for admitted provider failures; focused provider/web/API regression tests passed and commit `1a1858cd7f35abd5a7fa02ebb9c4e02eedf6f99b` was pushed to `main` and deployed as `lina-app-00019-vxh`. This evidence is transport/reliability scope only; it does not accept buffering UX or pedagogy.

E16: Owner-led natural live discovery, 2026-09-17, sanitized to avoid learner-content retention. In a fresh real-Luna Daily session, the Primary Tutor correctly classified an earlier method as `DID_NOT_HELP`, later honored an explicit visual request with `VISUAL_REPRESENTATION`, `CREATE`, `ADMITTED`, and a semantically coherent Canvas brief. The accepted typed Scene contained three intended line expressions and a classification interaction. The live screenshot plus source inspection showed the typed `PlotSurface` draws grid/axes/markers but not expressions; generic `SemanticButtons` binds SELECT to `block.elements[0]`, causing a real Studio event to claim selection of the first line without an actual visible target selection. Repeated generic interactions generated multiple Tutor turns; logs also captured `A newer Canvas Student interaction superseded this Chat Tutor result`, leaving one admitted Chat request without a Primary Tutor reply. The classification presentation exposed solved group membership before answer, and Tutor response to visual-delivery complaints fell back to text rather than repairing the visual. Later, after the Daily session expired under the existing 20m inactivity + 5m grace policy, repeated Send attempts reached the API as 404s while the old composer remained usable. These findings block clean real Canvas E2E acceptance; discovery continues in fresh sessions without product fixes until the owner ends the exploration round.


E17: Owner-led natural live discovery, 2026-09-17, conversation-momentum observation. In a fresh Arabic grammar practice sequence, one correct-answer Tutor acknowledgment ended while the instructional activity still had another component to cover, with no learner-facing next question, choice, suggested action, or explicit completion cue. The owner had to send a generic continuation message before the Tutor resumed the exercise. This is recorded as an IN_REVIEW child-guidance issue rather than a rule that every Tutor message must end with a question; more clean cross-subject cases are required before repair. No product code changed.

E18: Repeated conversation-momentum confirmation, 2026-09-17. In the same clean Arabic grammar live session, the owner again had to explicitly prompt the Tutor with a generic continuation message after the Tutor had acknowledged understanding/correctness but left the unfinished guided exercise without a next question, choice, action, or completion cue. This repetition confirms CONV-MOMENTUM-01 as an unresolved product behavior issue in active guided learning. It does not establish a rule that every Tutor reply must end with a question; cross-subject evidence is still needed to refine the repair semantics. No product code changed.

E19: Live session-expiry/reconnect continuity evidence, 2026-09-17. The Daily session was automatically closed after the configured inactivity plus grace window. Later Send attempts to that stale session returned 404. Reconnect first failed to resume the old session, then created a new empty Daily session. Read-only verification confirmed the old transcript remained durable with 48 messages while the new session began empty; the unsent learner draft was not admitted. This is a learner-facing continuity/draft-recovery defect, not transcript deletion. No product code changed.

E20: Additional owner-led live conversation review, 2026-09-20, using read-only production records from two later sessions. One trivial greeting-only session completed a normal Tutor reply in ~3.8s but still triggered both Segment Evidence and Personal Facts Luna calls when it later closed, retained only as evidence for deferred CALLS-01. A separate clean motivation-to-Science session showed: one successful Primary Tutor outlier at ~58.2s; later Science turns mostly ~8-11s; repeated Science process explanations with no Canvas request despite evaporation/water-cycle content and later confusion; multiple persisted `EXPLAIN_THEN_CHECK` strategies with no learner-visible check/action; and repeated unfinished-turn dead ends, confirming CONV-MOMENTUM-01 across subjects. The final explicit confusion turn remained `prior_method_relation=CONTINUATION`, so PED-ADAPT-01 is reopened for a bounded distinction between targeted clarification and failed-method adaptation. A repeated multiplication-related personalization phrase was also observed in motivation/study-strategy replies; it is recorded as IN_REVIEW only because its exact source authority was not audited here. No product code changed.

E21: REPAIR-SLICE-01 local implementation evidence, 2026-09-20, branch `codex/tutor-canvas-ab-01` at unchanged HEAD `1a1858cd7f35abd5a7fa02ebb9c4e02eedf6f99b`. Product files changed: `apps/api/routes/student.py`; `apps/web/components/daily-student/daily-student-app.tsx`; `apps/web/lib/daily-foreground-gate.ts`; `apps/web/lib/daily-session-recovery.ts`; `apps/web/lib/studio/agentic-canvas-contract.ts`; `apps/web/lib/studio/agentic-canvas.tsx`; `services/studio/protocol.py`; `services/studio/service.py`; `services/tutor/runtime.py`; `services/tutor/student_sessions.py`. Test files changed/added: `apps/web/lib/daily-foreground-gate.test.ts`; `apps/web/lib/daily-session-recovery.test.ts`; `apps/web/lib/studio/agentic-canvas.test.ts`; `tests/test_student_math_sessions.py`; `tests/test_studio_gateway_composition_postgres.py`; `tests/test_studio_make_ten_postgres.py`; `tests/test_studio_process_sequence_postgres.py`; `tests/test_studio_protocol_api_postgres.py`; `tests/test_studio_sentence_ordering_postgres.py`; `tests/test_studio_state_postgres.py`. Governance updates are this tracker and `project-state/PROJECT_STATE.md`; the pre-existing dirty `docs/LEARNING_PRODUCT_ROADMAP.md` was preserved and not edited by this slice. Verification: `npm run typecheck` passed; the TypeScript Daily/Studio gate passed `108/108`; focused PostgreSQL arbitration/session/protocol passed `9/9`; the broader affected Daily/Studio PostgreSQL files passed `107/107`; the final full disposable Python/PostgreSQL gate returned `1507 passed, 4 failed, 12 skipped` with exit code `1`, where the four failures are exactly the documented baseline (foundation table inventory, migration-head preflight, scheduled future Grade, Personal Facts reconciliation). This slice added three Python test functions; the remaining two-pass increase over E14's historical count already existed at the current HEAD. `python3 scripts/check_repository_truth.py` returned `REPOSITORY TRUTH CHECK: PASS`; `git diff --check` passed. Changes remain uncommitted. No merge, push, deploy, production database mutation, Clerk work, paid/live provider call or model change occurred.

E22: Independent REPAIR-SLICE-01 source review, 2026-09-20, on the same uncommitted worktree/HEAD. The core LearningSession-row foreground arbitration and target-bound SELECT direction are sound, and an independent rerun of eight focused PostgreSQL concurrency/protocol cases passed `8/8`; `npm run typecheck`, repository truth and `git diff --check` also passed. The slice is not yet accepted because three recovery gaps remain. (1) `apps/web/components/daily-student/daily-student-app.tsx` invokes the Canvas interaction Tutor stream only for `!result.replayed`; an exact idempotent replay can legitimately return the original interaction still `PENDING`, so a committed operation whose response was lost can be replayed without ever resuming its Tutor turn, leaving the shared foreground lane occupied. (2) `openDailySessionWithSingleReplacement` prevents more than one replacement only inside one invocation, while `open_or_resume_daily_session(..., learning_session_id=None)` always creates a new Daily session; if replacement creation commits but the response is lost, a later recovery retry can create another replacement, violating duplicate-replacement prevention. (3) after replacement, the UI merges the preserved old transcript and derives `activeSource` from that merged history; Student source assets are explicitly scoped to their original `learning_session_id`, so the replacement can carry an invalid old source reference into subsequent turns. The same recovery path also clears a still-local selected source file instead of preserving it as part of the unsent input state. Current web tests exercise helpers but do not prove the complete component recovery/rebind sequence. No product code was changed by this review; only governance/status documentation was updated. No push, merge, deploy, production DB mutation or model/provider call occurred.

E23: REPAIR-SLICE-01 E22 corrections, 2026-09-20, branch `codex/tutor-canvas-ab-01` at unchanged HEAD `1a1858cd7f35abd5a7fa02ebb9c4e02eedf6f99b`. The browser now resumes the exact returned Canvas interaction whenever its authoritative operation response is PENDING, including replay, while RUNNING/terminal responses do not start a stream; the existing server claim remains the atomic execution authority. Daily replacement requests now carry `replacement_for_session_id`; the server locks and validates the owned non-resumable source session and derives one deterministic Student-scoped replacement UUID, so a committed-but-lost response and later retry return the same replacement without a schema migration. Unknown, foreign and still-open references do not create a replacement; ordinary no-reference new-session creation remains unchanged. Replacement rebinding merges the old transcript only for presentation, derives `activeSource` only from replacement-session messages, and retains a still-local unsent file; the next source submission therefore uses the replacement session and carries the local file rather than the old asset ID. New E22 regressions passed `6/6` web and `5/5` focused PostgreSQL; the affected Daily/Studio web gate passed `110/110`, the affected seven-file PostgreSQL gate passed `142/142`, and `npm run typecheck` passed. The final named disposable PostgreSQL suite returned `1511 passed, 4 failed, 12 skipped` with exit code `1`; the failures are exactly the four documented baseline cases (foundation table inventory, migration-head preflight, scheduled future Grade, Personal Facts reconciliation). Changes remain uncommitted; no paid/live model call, production database mutation, Clerk work, commit, push, merge or deployment occurred. REPAIR-SLICE-01, SESSION-LIFECYCLE-01, TURN-ARB-01, CANVAS-SELECT-01 and CANVAS-QUEUE-01 remain IN_REVIEW pending independent review.

E24: Independent REPAIR-SLICE-01 acceptance review, 2026-09-20, same uncommitted worktree/HEAD. Source review confirmed the E23 corrections preserve the accepted one-foreground-lane invariant and target-bound SELECT behavior. Replayed Canvas operations now resume the exact interaction only when the authoritative response is `PENDING`; the public Tutor-interaction endpoint still atomically claims once, rejects a second claim, persists one Tutor execution/message, reaches `COMPLETED`, and releases the lane. Daily recovery now submits `replacement_for_session_id`; server-side Student locking plus a deterministic UUID derived from Student + ended-session identity makes committed-but-unobserved replacement creation retry-idempotent without a migration, while unknown/foreign/still-open references fail closed and ordinary no-reference creation remains separate. Replacement rebinding uses old transcript only for presentation, derives active source only from replacement-session messages, and preserves a still-local selected file so the next deliberate submission uses the replacement session without an old `source_asset_id`. Independent rerun of the three named E23 PostgreSQL functions produced `5 passed`; `npm run typecheck`, `python3 scripts/check_repository_truth.py`, and `git diff --check` passed. E23's full gate remains `1511 passed, 4 known baseline failures, 12 skipped`. No product code was changed by E24; only governance/state documentation was updated. No commit, push, merge, deploy, production DB mutation, Clerk work, or paid/live model call occurred. REPAIR-SLICE-01 and its four included issue IDs are locally DONE; live/production acceptance remains separate.

E25: REPAIR-SLICE-02 local implementation and clean integrated acceptance, 2026-09-20, branch `codex/tutor-canvas-ab-01` at unchanged HEAD `1a1858cd7f35abd5a7fa02ebb9c4e02eedf6f99b`, preserving the entire uncommitted E24 tree. Root causes: `PlotSurface` never consumed accepted expressions; `create_text_interaction` copied authoring `group_id` into learner `current_value` and the renderer grouped directly from solution data; Tutor context projected only part of the existing Canvas composition view and recovery wording did not make the truthful reload/retry/replace boundary actionable. Design: `apps/web/lib/studio/agentic-canvas-presentation.ts` adds a bounded no-eval parser for `y=c`/`y=mx+b` and exact viewport clipping; the renderer draws supported expressions with stable IDs/titles, preserves unsupported text as explicitly not drawn, retains Plot SELECT suppression, and exposes the existing reload callback. Text interaction authoring semantics remain in items/relations/order, new item state starts null, visible grouping reads only reduced element state, unassigned choices are visually separate from category targets, and Tutor projection carries both current state/action and bounded solution semantics. The existing `CanvasCompositionView` remains lifecycle authority; its Run/Job/Scene/failure/timestamp fields now reach Tutor context, while V2 guidance requires pending/no duplicate, terminal same-need RETRY, semantic-insufficiency-only REPLACE_SCENE, and no claim that `scene_ready` proves browser display. Product/test files changed for this slice: `apps/web/lib/studio/agentic-canvas-presentation.ts`, `apps/web/lib/studio/agentic-canvas.tsx`, `apps/web/lib/studio/agentic-canvas.test.ts`, `services/studio/agent/tools.py`, `services/studio/agentic_canvas.py`, `services/studio/tutor_context.py`, `services/tutor/runtime.py`, `runtime/tutor/visual-guidance-v2.md`, `tests/test_agentic_canvas_tools.py`, `tests/test_agentic_canvas_contract.py`, `tests/test_agentic_canvas_operations_postgres.py`, and `tests/test_studio_tutor_context.py`; governance changed only this tracker and PROJECT_STATE. Focused renderer/presentation tests passed `16/16`; focused contract/context tests passed `19/19`; visual/lifecycle/Tutor focused PostgreSQL gate passed `57/57`; affected Daily/Studio web gate passed `116/116`; broader affected Tutor/Studio/Canvas PostgreSQL gate passed `410/410`; the named clean local integrated acceptance gate passed `7/7`. Actual headless Chrome render evidence is `/private/tmp/lina-canvas-delivery-proof.png`: all three E16 lines are visibly distinct and clipped, the horizontal line is horizontal, categories are empty, choices are visibly separate, and Reload Workspace is present. `npm run typecheck` passed. Final `npm run test:python` returned exit `1` with `1514 passed, 4 failed, 12 skipped` in 77.58s; the three-pass increase over E23 is exactly the three new Python regression functions, and the four failures are the unchanged documented foundation-table, migration-head, future-Grade and Personal Facts baseline failures. Repository truth and `git diff --check` passed after the final governance update. Changes remain uncommitted. No production DB mutation, paid/live provider call, model change, Clerk work, commit, push, merge or deploy occurred. REPAIR-SLICE-02 and its three issue IDs are IN_REVIEW, not DONE; E2E-01 is IN_REVIEW for this local fixture-integrated scope only.

E26: Independent REPAIR-SLICE-02 source/evidence review, 2026-09-20, same uncommitted worktree/HEAD. The bounded Plot renderer is accepted in direction: `y=c` and `y=mx+b` use a no-eval parser, visible clipping, stable expression identity, explicit `(not drawn)` fallback for unsupported/out-of-view expressions, and Plot SELECT remains suppressed. Classification/grouping presentation also correctly separates authoring solution semantics from reduced learner state. Two blockers remain before local slice acceptance. First, generic `TEXT_INTERACTION` ORDERING now intentionally hides the authored answer by rotating the initial unassigned list, but `TextInteractionBlock` exposes MOVE only when groups exist and `SemanticButtons` does not expose MOVE/REORDER; therefore a typical ORDERING block with no groups is rendered as an unsolved `Current order` that the learner cannot actually reorder before Submit. Second, VISUAL-RECOVERY-01 is not behaviorally proven end-to-end: the new focused test only verifies failed Canvas lifecycle fields and recovery wording are present in the Primary Tutor model input. Existing Canvas lifecycle tests prove RETRY/REPLACE admission guards separately, but E25 does not yet exercise a deterministic Primary Tutor output choosing failed→RETRY, pending/running→no new composition, ready-but-client-missing→reload/no composition, or semantically-insufficient→REPLACE_SCENE and then confirm the corresponding admission outcome. Independent focused Python contract/context rerun passed `19/19`; `npm run typecheck`, repository truth and `git diff --check` passed. A direct Node test invocation was not counted because the local bare Node runner cannot load the `.tsx` module extension; that is a runner mismatch, not a product regression. No product code was changed by E26; only governance/state documentation was updated. No commit, push, merge, deploy, production DB mutation or provider/model call occurred.

E27: REPAIR-SLICE-02 E26 final-acceptance corrections, 2026-09-20, branch `codex/tutor-canvas-ab-01` at unchanged HEAD `1a1858cd7f35abd5a7fa02ebb9c4e02eedf6f99b`, preserving the uncommitted E24-E26 tree. ORDERING design: inspection confirmed the existing generic v1 reducer stores scalar element values but defines no durable list-position semantics, so no fake REORDER behavior was added. `textInteractionPresentation` returns no authored items for generic ORDERING and `TextInteractionBlock` renders a truthful non-actionable Tutor-chat fallback with no `Current order`, Submit, or semantic action controls. Recovery evidence now uses deterministic fixture providers through real `TutorRuntime`: FAILED same-need output persists RETRY with the exact failed Run decision base and admits exactly one retry; PENDING and RUNNING outputs request no brief/intent and preserve one authoritative Run; a ready suitable Scene plus a client visibility report produces the truthful Reload Workspace response with no new Run; and a materially different need persists REPLACE_SCENE with the exact active Scene ID/version and admits one replacement without mutating the prior Scene. The joined retry exposed one precise idempotency regression: exact accepted v12 source/digest replay was stale-checked after its Run became latest. Admission now returns the already-created exact Run before evaluating a new decision base; different/new stale decisions still fail closed, including the existing stale Scene/version regression. E27 product/test files: `apps/web/lib/studio/agentic-canvas-presentation.ts`, `apps/web/lib/studio/agentic-canvas.tsx`, `services/studio/agent/admission.py`, `apps/web/lib/studio/agentic-canvas.test.ts`, and `tests/test_agentic_canvas_lifecycle_postgres.py`; governance changed only this tracker. Verification: Agentic Canvas renderer/presentation `16/16`; recovery plus stale/idempotency `8/8`; focused Tutor/Canvas/context/visual-awareness `139/139`; broader affected Python/PostgreSQL files `191/191`; existing headless Chrome local renderer gate `7/7` with no page errors or bad assets; `npm run typecheck` passed. The final clean disposable suite returned exit `1` with `1519 passed, 4 failed, 12 skipped` in 68.07s. The five-pass increase over E25 is exactly the five new recovery cases (including PENDING/RUNNING parameterization), and the four failures are the unchanged documented foundation-table, migration-head, future-Grade, and Personal Facts baselines. Repository truth and `git diff --check` passed after this tracker update. Changes remain uncommitted. No production DB mutation, paid/live provider call, model change, Clerk work, commit, push, merge, or deploy occurred. REPAIR-SLICE-02, CANVAS-RENDER-01, CANVAS-ANSWER-LEAK-01, VISUAL-RECOVERY-01, and E2E-01 remain IN_REVIEW for independent review; no DONE status is claimed.

E28: Independent REPAIR-SLICE-02 acceptance review, 2026-09-20, same uncommitted worktree/HEAD. Source review confirms the E27 ORDERING decision is truthfully fail-closed: generic ORDERING exposes neither authored item order nor `Current order`, Submit, MOVE/REORDER controls, and presents only the Tutor-chat fallback because the current generic reducer has no durable list-position contract. Recovery review confirms deterministic fixture outputs traverse real `TutorRuntime` persistence plus `admit_agentic_canvas_brief`: FAILED same-need binds RETRY to the exact failed Run and admits one retry; PENDING/RUNNING create no second Run; ready-but-client-missing produces Reload Workspace with no composition change; semantic insufficiency binds REPLACE_SCENE to exact Scene ID/version and admits one replacement; stale new decisions still fail closed. The exact accepted source/digest replay shortcut is safe in current call sites because it returns only the already-created Run and enqueues no new work; runtime callers use admission as a side-effect boundary and do not treat the returned historical Run as a new composition. Independent rerun: Agentic Canvas web/presentation `16/16`; E27 recovery/stale/idempotency PostgreSQL cases `8/8`; `npm run typecheck` passed; repository truth and `git diff --check` passed. E27's full disposable suite remains `1519 passed, 4 known baseline failures, 12 skipped`, and its Chrome gate remains `7/7` with no page errors/bad assets. No product code was changed by E28; only governance/state documentation was updated. No commit, push, merge, deploy, production DB mutation, Clerk work, or paid/live provider call occurred. REPAIR-SLICE-02 and CANVAS-RENDER-01/CANVAS-ANSWER-LEAK-01/VISUAL-RECOVERY-01 are locally DONE. E2E-01 remains IN_REVIEW only for controlled real-Luna/live acceptance after an authorized deployment.

External evidence paths are local references and may need recovery if temporary directories are cleaned. Missing artifacts mean the proof must be recovered or qualified, not that a result can be invented. Never put keys, cookies, auth traces or private learner material into this register.

## Snapshot identity and handoff discipline

Before this documentation-only tracker update, the same working copy had tracked-only diff SHA-256 `455ff0864ca1132830f83fffe428169f7f1c3e216db0e01b553ebcea9e5e35b0` and combined tracked-plus-three-new-files SHA-256 `26912a7bf64dd81266d26503dfcd471623f7aab9fbacde519571dd16212b3098`. Different scopes, not different product versions. Adding this tracker and its documentation pointers changes a full-worktree fingerprint; do not reuse the old fingerprint as the identity of the enlarged documentation diff.

For each status update, record the exact code commit plus applicable diff/artifact hash, command/result or source comparison, and which acceptance limit remains. Preserve the four known baseline failures explicitly; do not report an entirely green full suite while they remain.

Immediate next action: preserve the independently accepted uncommitted E24/E28 repair tree in a reviewed branch commit if the Product Owner authorizes it. After preservation, the next logical local repair slice is teaching-flow integrity: CONV-MOMENTUM-01 + STRATEGY-FIDELITY-01 + PED-ADAPT-01. Controlled real-Luna/live E2E, PERF/UX-BUFFER, CALLS-01, AUTH-01 and IMG-V2-01 remain separate; commit/push/merge/deploy require explicit Product Owner direction.

AB-B source implementation and V01 local test compatibility are accepted in E10-E12. Keep V02/E2E/UX-BUFFER separate and unresolved until their own evidence/decision exists.

Every Codex progress reply should identify changed tracker IDs, old -> new status, evidence and next action. Do not report an item as DONE merely because a prompt mentions it. Do not reset other completed items or mark a deferred study as a mandatory repair.
