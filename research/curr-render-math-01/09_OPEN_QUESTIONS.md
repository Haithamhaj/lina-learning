# CURR-RENDER-MATH-01 — Open Questions and Gates

## Blocking before renderer implementation

1. Product Owner must promote a specific, bounded implementation task; this research pack is non-authoritative.
2. Studio foundations and Make-Ten are accepted at the correction baseline; reuse their exact Event/Snapshot and Subject Capability boundaries. Each new Grade 5 activity still requires its own approved contract, fixtures, validator and accessibility verification.
3. Each `EVALUATE BEFORE CUSTOM BUILD` reuse-catalog item relevant to the selected slice needs an `ADOPT`, `PARTIAL ADOPT`, or `REJECT` record. Current direction does not authorize a package install.
4. A first renderer must have an explicit learner outcome, fixture set, validator contract, and accessibility/mobile acceptance criteria.
5. Rights review must approve any source asset before content ingestion, copying, adaptation, or runtime use; research links alone are not permission.

## Non-blocking

- Exact McGraw edition and pacing are not required to preserve the concept/renderer catalog.
- No textbook copy, Khan content, or MLC app code is needed for a useful foundation.
- Full Grade 4/6 curriculum research is unnecessary; cross-grade labels are estimates for prioritization.
- A Canvas Specialist, generated image, Vision, rich 3D, JSXGraph, Konva, and MathLive are not prerequisites for a deterministic React/SVG proof.

## Requires Product Owner decision

1. Select the first implementation proof objective and initial renderer batch from [06_COVERAGE_AND_PRIORITY.md](06_COVERAGE_AND_PRIORITY.md); this pack recommends `number_line`, `place_value_workspace`, `fraction_model_workspace`, and `division_workspace` but does not authorize them.
2. The earlier feed/event-history architecture question is resolved by accepted Studio. Select the bounded first activity in each proposed family; interactive work uses the accepted Event/Snapshot/feed authority, with display-only allowed when useful.
3. For each newly approved Activity, declare its bounded RECORD_ONLY and Tutor-triggering actions using the accepted contract; these never automatically become Learning Intelligence evidence.
4. Decide the source-asset approval workflow for the future Trusted Educational Reference Pack, including commercial product posture.

## Requires book / McGraw data

- exact edition/year/ISBN and volumes;
- table of contents, sequence, school pacing, and local vocabulary;
- authorized source provenance and any license/access conditions;
- the small set of real page/handwriting patterns that would justify a reconstruction configuration.

## Requires proof spike

1. Plain React/SVG versus JSXGraph for a specific coordinate/geometry interaction.
2. Plain React/SVG versus constrained Konva only if spatial cube/annotation editing cannot meet a11y/mobile acceptance.
3. Native controlled input versus MathLive for a defined expression-entry task.
4. The common Event/Snapshot/reconnect foundation has accepted Make-Ten evidence. Each future Grade 5 activity must verify its own cancellation, stale rejection and reconstruction; foundational acceptance does not accept its mathematics.

## Requires real Lina use

- Whether visual/interactive representation actually clarifies the concepts where this map predicts high value.
- Whether Lina prefers bars, number lines, grids, or concise notation for a given state; this should inform Tutor choice, not turn a static preference into authority.
- Whether the initial batch's support level, interaction friction, touch targets, Arabic/English/mixed-language behavior, and accessible text alternatives work in natural homework use.
- Whether photo side-by-side/annotation/reconstruction adds enough value to justify the separately gated Vision path.

## Correction limits and separately recorded issues

- Node 12 deliberately retains decimal-by-whole scope; decimal divisors are part of the broader 5.NBT.B.7 wording, but no complete decimal-divisor activity is specified or counted. Recurring quotients/rounding policy waits for a later bounded decision; no new release blocker is created.
- Node 1 now correctly maps 5.OA.A.1–2 but remains partial in the proposed catalog: grouping/evaluation and bounded operation trees do not establish arbitrary interpretation/writing. A future explicit fixture/structural-relation contract is needed for full node support.
- Coverage audit qualifications: node 8 lacks the algorithm/fluency part of its stated aim in the area-only proposal; node 2 lacks a complete generation/relationship/plotting activity; contextual nodes 15, 20, 23, 27 and 30 require source interpretation. Counts disclose these gaps without adding implementation scope.
- Separately discovered outside the named corrections: node 19's historical reference uses 5.NF.B.4a for fractional rectangle area; the official section is 5.NF.B.4b (printed p.36). Node 31's classification aim also merits checking against 5.G.B.4 rather than relying on .3 alone. The original rows are retained pending direction; their quantitative coverage is bounded representation support, not certification of all historical citations. Impact: do not present this run as a full revalidation of every unchanged Grade 5 mapping.
- The first batch remains a diversity-of-state-model recommendation. Its first-slice coverage is smaller than the union of all capabilities proposed for those families; neither percentage predicts learner benefit, fluency, or actual McGraw coverage.
