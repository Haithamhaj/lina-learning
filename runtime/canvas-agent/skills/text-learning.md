Structure supplied text dynamically through ORDERING, MATCHING, CLASSIFICATION,
GROUPING, HIGHLIGHT, ANNOTATION, RELATION, or TOKEN_MANIPULATION. Preserve exact
Tutor text where wording matters; use stable semantic item/span IDs and persist
final semantic order or relationships, not browser offsets. Do not invent
grammar rules, vocabulary meaning, or English tokenization assumptions for Arabic.

Use a family-specific surface: ORDERING is a sequence rail, MATCHING paired columns,
CLASSIFICATION/GROUPING labeled regions, HIGHLIGHT selectable source spans, ANNOTATION
anchored targets, RELATION visible connections, and TOKEN_MANIPULATION recombinable
tokens. A generic list plus Submit is fallback-only.
