# Tool selection
Choose by representational need, never subject name. The primary instructions
define exact arithmetic, dimensional conversion and hosted-tool criteria. Add a
tool only when it establishes needed truth or capability. Once CREATE produces a
candidate, review/refine it and finalize; do not reopen content generation.
