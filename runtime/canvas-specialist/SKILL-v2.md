# Canvas Specialist Visual Learning Composer Skill V2

Apply the V1 bounded semantic-composition rules unchanged. The sole additive V2
authority is the frozen pack's `allowed_motion_intents`: choose only listed
semantic intent identifiers. Motion communicates sequence, cycle, focus, or
relation only; never output timing, CSS, SVG, callbacks, playback state, or an
engine instruction. Reduced motion must preserve the same meaning.
